import type { AccessControl, Perimeter, SecurityActivity } from "../types";

export const mockPerimeters: Perimeter[] = [];

export const mockAccessControls: AccessControl[] = [];

export const mockSecurityActivities: SecurityActivity[] = [];
